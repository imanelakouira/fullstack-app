# 1- commandes run backend 
cd backend
pip install -r requirements.txt
python app.py
# 1- commandes run frontend créer par commande:(npm create vite@latest frontend --template react)
 cd..
 cd frontend
 npm install
 npm run dev
